<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gronthomongol">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/pasheachi_logo_modified"
        android:label="গ্রন্থমঙ্গল"
        android:roundIcon="@drawable/pasheachi_logo_modified"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.auth.test.AuthActivity"></activity>
        <activity android:name=".ui.main.admin.OrderDetailsAdminActivity" />

        <meta-data
            android:name="android.app.searchable"
            android:resource="@xml/searchable" />

        <activity android:name=".ui.main.admin.AddBookActivity" />
        <activity android:name=".ui.main.admin.RequestListActivity" />
        <activity
            android:name=".ui.main.admin.BooklistAdminActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity android:name=".ui.main.admin.BookDetailsActivity" />
        <activity android:name=".ui.auth.ForgotPasswordActivity" />
        <activity android:name=".ui.main.user.ViewOrdersActivity" />
        <activity android:name=".ui.main.user.OrderDetailsActivity" />
        <activity android:name=".ui.main.user.PlaceOrderActivity" />
        <activity android:name=".ui.main.user.RequestBookActivity" />
        <activity
            android:name=".ui.main.user.BooklistActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity android:name=".ui.auth.SignUpActivity" />
        <activity android:name=".ui.auth.SignInActivity" />
        <activity android:name=".ui.welcome.WelcomeScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name="com.backendless.push.BackendlessFCMService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>